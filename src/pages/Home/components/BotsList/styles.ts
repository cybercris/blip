import styled from 'styled-components'

export const BlocksContainer = styled.ul`
  display: flex;
  gap: 1.5rem;
  flex-wrap: wrap;
`

export const ListContainer = styled.ul`
  display: flex;
  flex-direction: column;
  gap: 1rem;
  flex-wrap: wrap;
`
